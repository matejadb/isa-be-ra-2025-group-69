# Lombok Configuration
# This helps resolve compatibility issues with newer JDK versions

# Enable lombok annotation processing
lombok.addLombokGeneratedAnnotation = true

# Configure lombok to be compatible with newer Java versions
config.stopBubbling = true
